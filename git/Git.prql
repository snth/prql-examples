# Requires mergestat

# Defines tables with less columns that display better in the terminal

let commits = (
  from commits
  select {hash, message, author_name, author_email, parents}
)

let refs = (from refs)

let stats = (from stats)

let files = (from files)

let blame = (from blame)
