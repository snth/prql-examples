
let top = (
  from hackernews_top
)

let top_score = (
  from hackernews_top
  select {score, comments=descendants ?? 0, title, url, id}
  sort {-score}
)

let top_comments = (
  from hackernews_top
  select {score, comments=descendants ?? 0, title, url, id}
  sort {-comments}
)
